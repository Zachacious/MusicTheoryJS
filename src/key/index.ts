/**
 * @module key
 * Keys: signatures, diatonic harmony with function tags, and secondary /
 * substitute dominants for major and (three-variant) minor keys.
 */

export {
  type Key,
  type KeyHarmony,
  type MajorKey,
  type MinorKey,
  key,
  majorKey,
  minorKey,
  tryKey,
} from "./key";
